- name: set up mongdb server
  hosts: mongodb
  become: yes
  tasks:

   - name: copy mongodb repo
     ansible.builtin.copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo
     
  #  - name: mongodb
  #    ansible.builtin.dnf:
  #     name: mongodb
  #     state: present

